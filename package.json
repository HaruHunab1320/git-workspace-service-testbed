{
  "name": "health-endpoint-service",
  "version": "1.2.0",
  "description": "Service with health monitoring endpoint",
  "main": "src/app.js",
  "scripts": {
    "test": "jest --verbose",
    "test:unit": "jest tests/unit --verbose"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
