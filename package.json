{
  "name": "git-workspace-service-testbed",
  "version": "1.0.0",
  "description": "Service testbed with health endpoint",
  "main": "src/index.js",
  "scripts": {
    "test": "jest --verbose"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
