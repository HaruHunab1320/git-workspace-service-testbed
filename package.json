{
  "name": "health-endpoint-service",
  "version": "1.0.0",
  "description": "API service with health endpoint",
  "main": "src/index.js",
  "scripts": {
    "test": "jest --verbose",
    "start": "node src/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
