// Database module stub — agent "alpha" will provide the real implementation.
// Exports a default object with a query() method for running SQL queries.

const db = {
  async query(sql) {
    // Placeholder: real implementation should connect to a database
    throw new Error("db.query not implemented");
  },
};

export default db;
