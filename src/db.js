/**
 * Database module stub.
 * Alpha agent should implement the actual database connection logic.
 * The ping() function must resolve to true if the DB is reachable,
 * or reject / resolve to false if it is not.
 */

async function ping() {
  // TODO: Alpha to implement — e.g., a lightweight SELECT 1 or equivalent
  throw new Error('Not implemented');
}

module.exports = { ping };
