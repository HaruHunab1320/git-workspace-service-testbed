// Database module stub — alpha agent will provide the real implementation.
// Exports a checkConnection() function that returns a promise resolving to a boolean.

async function checkConnection() {
  // Default: no database configured yet
  return false;
}

module.exports = { checkConnection };
